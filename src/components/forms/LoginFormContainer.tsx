import { Header, ItemWrapper, Logo, Title } from './LoginFormPresenter';
import logo from '../../assets/images/logo.png';

// 수정 중
const LoginFormContainer = () => {
  return (
    <form>
      <input />
      <input />
      <button>Let's start!</button>
    </form>
  );
};

export default LoginFormContainer;
